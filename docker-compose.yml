services:
  docs:
    image: ghcr.io/verbatra/verbatra-docs:latest
    container_name: verbatra_docs
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production