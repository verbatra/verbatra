---
"@verbatra/studio": patch
---

Carry a third-party format identifier through the project snapshot, so a project configured with a
`custom:` format renders in the dashboard instead of failing to typecheck against the built-in
format set.
