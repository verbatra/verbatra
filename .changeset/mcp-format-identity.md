---
"@verbatra/mcp": patch
---

Report a third-party format identifier in the project snapshot tool, so a project configured with a
`custom:` format is described accurately instead of being typed against the built-in format set.
