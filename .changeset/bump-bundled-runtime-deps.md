---
"@verbatra/sdk": patch
---

Bump bundled runtime dependencies (`@anthropic-ai/sdk`, `@google/genai`, `openai`, `@formatjs/icu-messageformat-parser`, `@xmldom/xmldom`, `zod`) to their latest releases; no behavior change observed.
