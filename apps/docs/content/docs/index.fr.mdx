---
title: Introduction
description: Ce qu'est verbatra, le problème qu'il résout, et comment la CLI, le SDK et Studio s'articulent. Traduis uniquement les clés qui ont changé, avec des contrôles d'intégrité sur chaque résultat.
---

<DocsHomeHero
  eyebrow="Documentation"
  headline="Garde chaque fichier de locale aligné sur ta source."
  lead="verbatra traduit tes fichiers de locale i18n de façon incrémentale : il n'envoie que les chaînes nouvelles et modifiées au fournisseur que tu configures, vérifie chaque résultat, et réécrit les fichiers en place."
  primary={{ label: "Démarrage rapide", href: "/docs/your-first-translation" }}
  secondary={{ label: "Référence CLI", href: "/docs/cli" }}
/>

<DocsHomeBody>

## Le problème

Tu maintiens à la main une seule locale source. Tous les autres fichiers de locale dérivent à mesure que cette source évolue : les nouvelles clés manquent dans les cibles, les chaînes source modifiées laissent derrière elles des traductions périmées, et les clés supprimées traînent en orphelines. Rattraper à la main ne passe pas à l'échelle, et tout retraduire à chaque changement est lent, coûte des tokens fournisseur pour des chaînes qui étaient déjà bonnes, et remue du texte que ton équipe a déjà relu.

## Comment verbatra le résout

Un fichier de verrouillage commité, `verbatra.lock.json`, enregistre pour chaque clé traduite le hash de la chaîne source dont sa traduction provient. Chaque exécution lit la locale source, compare chaque cible avec elle et avec cette référence, et n'envoie au fournisseur que les clés manquantes et modifiées. Les clés encore à jour sont laissées tranquilles : lance `translate` deux fois de suite et la seconde exécution ne fait aucun appel fournisseur.

Chaque chaîne renvoyée par le fournisseur est vérifiée avant d'être écrite : une traduction qui échoue à l'une des vérifications de la barrière d'intégrité est retenue au lieu d'être livrée, puis retentée à l'exécution suivante. Les commandes en lecture seule `check` et `diff` rapportent la dérive sans rien écrire, pour que ta CI puisse bloquer sur leurs codes de sortie.

verbatra lit et écrit douze formats (JSON i18next, vue-i18n, next-intl et ngx-translate, XLIFF, YAML, Flutter ARB, les properties Java/Spring, le `.strings` d'Apple, les Xcode String Catalogs, le `strings.xml` d'Android et le `.po`/`.pot` gettext) et traduit via six fournisseurs (Anthropic, OpenAI, Gemini, DeepL, Google Cloud Translation, et tout serveur compatible OpenAI, y compris local) derrière une seule interface. Voir [Formats](/docs/formats) et [Fournisseurs](/docs/providers).

## Les paquets

verbatra est livré en quatre paquets npm :

- **@verbatra/cli** fournit le binaire `verbatra` avec dix commandes : `init`, `translate`, `watch`, `check`, `diff`, `doctor`, `export`, `import`, `studio` et `mcp`. C'est une fine couche au-dessus du SDK, donc tout ce qu'elle fait est aussi disponible programmatiquement. Commence ici.
- **@verbatra/sdk** est le moteur en dessous : `translate()` en un coup, `watch()` de longue durée, `check()`, `diff()` et `doctor()` en lecture seule, l'aller-retour de classeur Excel pour les traducteurs humains, et le chargement de la configuration. Utilise-le directement depuis des scripts, des outils de build ou la CI. Voir [le SDK](/docs/sdk).
- **@verbatra/studio** est Verbatra Studio, un tableau de bord local sur ton projet avec édition intégrée et une file de révision. La CLI le démarre avec `verbatra studio` ; la dépense fournisseur depuis Studio reste désactivée sauf si tu passes `--allow-spend`. Voir [Réviser les traductions dans Studio](/docs/review-in-studio).
- **@verbatra/mcp** est un serveur MCP en stdio qui expose le statut de traduction, le glossaire et les outils d'édition de verbatra à un client MCP. La CLI le démarre avec `verbatra mcp`. Voir [le serveur MCP](/docs/cli/mcp).

## Où aller ensuite

<DocsHomePaths
  cards={[
    {
      tag: "Nouveau ici",
      label: "Ta première traduction",
      body: "Installe la CLI, génère une configuration, définis ta clé, et traduis.",
      href: "/docs/your-first-translation",
      primary: true,
    },
    {
      tag: "Déjà en place",
      label: "Ajouter une langue",
      body: "Ajoute une locale à ta configuration et laisse une exécution la remplir.",
      href: "/docs/add-a-language",
    },
    {
      tag: "Sous le capot",
      label: "Comment ça marche",
      body: "Le pipeline : comparer avec la référence du fichier de verrouillage, traduire, vérifier, écrire.",
      href: "/docs/how-it-works",
    },
    {
      tag: "Avec un agent IA",
      label: "Configurer avec l'IA",
      body: "Donne à Claude Code, Cursor ou tout agent de codage un prompt prêt à copier-coller.",
      href: "/docs/start-with-ai",
    },
  ]}
/>

</DocsHomeBody>
