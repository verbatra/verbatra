---
title: Einführung
description: Was verbatra ist, welches Problem es löst und wie CLI, SDK und Studio zusammenspielen. Übersetze nur die Keys, die sich geändert haben, mit Integritätsprüfungen für jedes Ergebnis.
---

<DocsHomeHero
  eyebrow="Dokumentation"
  headline="Halte jede Locale-Datei im Gleichschritt mit deiner Quelle."
  lead="verbatra übersetzt deine i18n-Locale-Dateien inkrementell: Es sendet nur die neuen und geänderten Strings an den Provider, den du konfigurierst, prüft jedes Ergebnis und schreibt die Dateien direkt zurück."
  primary={{ label: "Schnellstart", href: "/docs/your-first-translation" }}
  secondary={{ label: "CLI-Referenz", href: "/docs/cli" }}
/>

<DocsHomeBody>

## Das Problem

Du pflegst eine Quell-Locale von Hand. Jede andere Locale-Datei driftet ab, während diese Quelle sich weiterentwickelt: Neue Keys fehlen in den Zielen, bearbeitete Quellstrings hinterlassen veraltete Übersetzungen, und gelöschte Keys bleiben als Waisen zurück. Von Hand aufzuholen skaliert nicht, und bei jeder Änderung alles neu zu übersetzen ist langsam, kostet Provider-Tokens für Strings, die schon in Ordnung waren, und wirbelt Texte durcheinander, die dein Team bereits geprüft hat.

## Wie verbatra es löst

Eine committete Lock-Datei, `verbatra.lock.json`, hält für jeden übersetzten Key den Hash des Quellstrings fest, aus dem seine Übersetzung entstanden ist. Jeder Lauf liest die Quell-Locale, vergleicht jedes Ziel mit ihr und mit dieser Baseline und sendet nur die fehlenden und geänderten Keys an den Provider. Keys, die noch aktuell sind, bleiben unangetastet: Führe `translate` zweimal hintereinander aus, und der zweite Lauf macht überhaupt keinen Provider-Aufruf.

Jeder String, den der Provider zurückgibt, wird vor dem Schreiben geprüft: Eine Übersetzung, die eine der Prüfungen des Integritäts-Gates nicht besteht, wird zurückgehalten statt ausgeliefert und beim nächsten Lauf erneut versucht. Die schreibgeschützten Befehle `check` und `diff` melden Drift, ohne etwas zu schreiben, sodass deine CI anhand ihrer Exit-Codes ein Gate setzen kann.

verbatra liest und schreibt zwölf Formate (i18next-, vue-i18n-, next-intl- und ngx-translate-JSON, XLIFF, YAML, Flutter ARB, Java/Spring-Properties, Apple-`.strings`, Xcode String Catalogs, Android-`strings.xml` und gettext-`.po`/`.pot`) und übersetzt über sechs Provider (Anthropic, OpenAI, Gemini, DeepL, Google Cloud Translation und jeden OpenAI-kompatiblen Server, auch lokale) hinter einer einzigen Schnittstelle. Siehe [Formate](/docs/formats) und [Provider](/docs/providers).

## Die Pakete

verbatra erscheint als vier npm-Pakete:

- **@verbatra/cli** liefert das `verbatra`-Binary mit zehn Befehlen: `init`, `translate`, `watch`, `check`, `diff`, `doctor`, `export`, `import`, `studio` und `mcp`. Es ist ein dünner Wrapper über dem SDK, alles, was es tut, ist also auch programmatisch verfügbar. Starte hier.
- **@verbatra/sdk** ist der Motor darunter: einmaliges `translate()`, langlaufendes `watch()`, schreibgeschützte `check()`, `diff()` und `doctor()`, der Excel-Arbeitsmappen-Roundtrip für menschliche Übersetzerinnen und das Laden der Konfiguration. Nutze es direkt aus Skripten, Build-Tools oder CI. Siehe [das SDK](/docs/sdk).
- **@verbatra/studio** ist Verbatra Studio, ein lokales Dashboard über deinem Projekt mit eingebautem Bearbeiten und einer Review-Warteschlange. Die CLI startet es mit `verbatra studio`; kostenpflichtige Provider-Aufrufe aus Studio bleiben aus, solange du nicht `--allow-spend` übergibst. Siehe [Review in Studio](/docs/review-in-studio).
- **@verbatra/mcp** ist ein stdio-MCP-Server, der den Übersetzungsstatus, das Glossar und Bearbeitungs-Tools von verbatra für einen MCP-Client bereitstellt. Die CLI startet ihn mit `verbatra mcp`. Siehe [den MCP-Server](/docs/cli/mcp).

## Wohin als Nächstes

<DocsHomePaths
  cards={[
    {
      tag: "Neu hier",
      label: "Deine erste Übersetzung",
      body: "Installiere die CLI, erzeuge eine Konfiguration, hinterlege deinen API-Schlüssel und übersetze.",
      href: "/docs/your-first-translation",
      primary: true,
    },
    {
      tag: "Schon eingerichtet",
      label: "Eine Sprache hinzufügen",
      body: "Trage eine Locale in deine Konfiguration ein und lass einen Lauf sie füllen.",
      href: "/docs/add-a-language",
    },
    {
      tag: "Unter der Haube",
      label: "Wie es funktioniert",
      body: "Die Pipeline: Vergleich mit der Lock-Baseline, übersetzen, prüfen, schreiben.",
      href: "/docs/how-it-works",
    },
  ]}
/>

</DocsHomeBody>
