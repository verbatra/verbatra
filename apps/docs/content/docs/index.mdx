---
title: Introduction
description: What verbatra is, the problem it solves, and how the CLI, SDK, and Studio fit together. Translate only the keys that changed, with integrity checks on every result.
---

<DocsHomeHero
  eyebrow="Documentation"
  headline="Keep every locale file in step with your source."
  lead="verbatra translates your i18n locale files incrementally: it sends only the new and changed strings to the provider you configure, checks every result, and writes the files back in place."
  primary={{ label: "Quick start", href: "/docs/your-first-translation" }}
  secondary={{ label: "CLI reference", href: "/docs/cli" }}
/>

<DocsHomeBody>

## Where to go next

<DocsHomePaths
  cards={[
    {
      tag: "New here",
      label: "Your first translation",
      body: "Install the CLI, scaffold a config, set your key, and translate.",
      href: "/docs/your-first-translation",
      primary: true,
    },
    {
      tag: "Already set up",
      label: "Add a language",
      body: "Drop a locale into your config and let one run fill it in.",
      href: "/docs/add-a-language",
    },
    {
      tag: "Under the hood",
      label: "How it works",
      body: "The pipeline: diff against the lock baseline, translate, verify, write.",
      href: "/docs/how-it-works",
    },
    {
      tag: "With an AI agent",
      label: "Set up with AI",
      body: "Point Claude Code, Cursor, or any coding agent at a copy-pasteable prompt.",
      href: "/docs/start-with-ai",
    },
  ]}
/>

## What verbatra does

Hand-maintaining one source locale means every other locale file drifts: new keys go missing from the targets, edited strings leave stale translations behind, and deleted keys linger as orphans. verbatra closes that gap by diffing every target against a committed lock file, `verbatra.lock.json`, and sending only what changed to the provider you configure; a second run with nothing new to translate makes no provider call at all. Every result is checked before it is written, so a broken placeholder is withheld and retried, never shipped. See [How it works](/docs/how-it-works) for the full pipeline.

verbatra reads and writes twelve formats and translates through six providers behind one interface. See [Formats](/docs/formats) and [Providers](/docs/providers).

## The packages

<DocsHomeFeatures
  features={[
    {
      title: "@verbatra/cli",
      body: "The verbatra binary: ten commands, a thin wrapper over the SDK. Start here.",
      href: "/docs/cli",
    },
    {
      title: "@verbatra/sdk",
      body: "The engine underneath: translate, watch, check, diff, doctor, plus the Excel workbook round-trip.",
      href: "/docs/sdk",
    },
    {
      title: "@verbatra/studio",
      body: "A local dashboard with built-in editing and a review queue. Provider spend stays off unless you opt in.",
      href: "/docs/review-in-studio",
    },
    {
      title: "@verbatra/mcp",
      body: "A stdio MCP server exposing translation status, glossary, and editing tools to an MCP client.",
      href: "/docs/cli/mcp",
    },
  ]}
/>

</DocsHomeBody>
