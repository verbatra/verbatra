---
title: verbatra check
description: Signale les clés manquantes ou obsolètes par locale sans écrire de fichiers.
---
Signale, par locale cible, combien de clés sont manquantes, obsolètes ou à jour, puis sort avec un code sur lequel une étape CI peut se brancher. `check` exécute la même lecture et la même comparaison que [`verbatra translate`](/docs/cli/translate) effectue juste avant d'appeler un fournisseur, puis s'arrête : aucun appel de fournisseur, aucun fichier écrit, aucune mise à jour du [fichier de verrouillage](/docs/the-lock-file) et aucune clé d'API requise.

## Synopsis

```bash
verbatra check [flags]
```

## Options

| Option | Argument | Défaut | Effet |
| --- | --- | --- | --- |
| `--cwd` | `<path>` | répertoire courant | résout la config et les fichiers de locale depuis ce répertoire |
| `--config` | `<path>` | recherche automatique | charge ce fichier de config au lieu d'en chercher un |
| `--locales` | `<list>` | toutes les cibles configurées | sous-ensemble de locales cibles à vérifier, séparées par des virgules |
| `--consistency` | aucun | désactivé | signale aussi, par locale, chaque chaîne source traduite de plusieurs façons sous des clés différentes ; simple rapport, le code de sortie ne change jamais |
| `--json` | aucun | désactivé | affiche une [enveloppe JSON](/docs/ci-and-exit-codes#sortie-json) sur stdout portant le résumé de vérification sous `result`, ou le code d'erreur si l'exécution échoue ; la ligne d'erreur lisible par un humain va toujours sur stderr |

Une clé est manquante quand la source l'a et pas la cible ; obsolète quand la chaîne source a changé depuis sa dernière traduction. Une locale est synchronisée quand elle n'a ni l'un ni l'autre. Une valeur `--locales` vide ou inconnue est rejetée avec la sortie `2` plutôt que de ne rien vérifier en silence, donc une coquille dans un nom de locale ne laisse jamais passer un contrôle CI. Tu veux les listes de clés derrière ces comptes ? C'est [`verbatra diff`](/docs/cli/diff).

## Rapport de cohérence

<AvailableFrom version="0.11.0" />

`--consistency` ajoute un second rapport en lecture seule : dans chaque locale cible, chaque chaîne source que deux clés ou plus traduisent différemment. Chaque résultat nomme la chaîne source, chaque traduction distincte et les clés qui la portent, pour que tu choisisses une formulation et alignes le reste à la main. Rien n'est modifié à ta place.

C'est un rapport, rien de plus. Il ne change jamais le code de sortie, ne désynchronise jamais une locale, ne retient ni ne réécrit aucune valeur et ne marque aucune clé pour relecture. Comme le reste de `check`, il n'appelle aucun fournisseur et n'a besoin d'aucune clé API.

La comparaison suit une seule règle, appliquée à la source comme à la traduction :

- Les valeurs sont comparées après normalisation Unicode NFC, conversion de `\r\n` en `\n` et suppression des espaces en début et en fin, donc `"Save "` et `"Save"` comptent comme la même chaîne. Les espaces à l'intérieur de la valeur et la casse comptent.
- Seules les clés à jour sont comparées. Une clé manquante n'a pas encore de traduction, et la traduction d'une clé obsolète appartient à un texte source plus ancien, que `check` signale déjà.
- Les clés ne sont jamais regroupées si leur description, leur sens, leur marque de pluriel ou leur `msgctxt` gettext diffèrent, car ces métadonnées existent justement pour permettre une autre traduction.
- Les formes plurielles sont comparées forme par forme. Un format qui range chaque forme sous sa propre clé (i18next, Apple `.stringsdict` et `.xcstrings`, Android `<plurals>`, gettext `msgstr[n]`) ne regroupe jamais entre elles les formes `one` et `other` d'une même clé, même si leur texte source est identique ; la même forme sous des clés différentes reste comparée.
- Une traduction identique à sa propre source n'est pas un résultat ici ; une exécution de traduction la marque déjà pour relecture avec `EQUALS_SOURCE`.

Les groupes sont triés par chaîne source, les traductions par valeur et les clés par nom, donc le même catalogue produit toujours le même rapport. Avec `--json`, chaque locale de `result.locales` porte un tableau `inconsistencies` de `{ source, context?, description?, meaning?, isPlural, pluralForm?, translations: [{ value, keys }] }` ; sans le flag, le champ est absent.

### Limites connues

- Les variantes de contexte i18next sont signalées. i18next encode un contexte comme suffixe de clé (`invite_male`, `invite_female`), impossible à distinguer d'une clé snake_case ordinaire, donc deux variantes de contexte au même texte source mais traduites différemment apparaissent comme un résultat. Si ton format le permet, donne-leur une description ou un sens différents pour qu'elles ne soient jamais regroupées ; sinon, ignore le résultat.
- Les entrées gettext marquées `#, fuzzy` comptent comme des traductions. L'adaptateur gettext ne conserve pas le marqueur fuzzy, donc un `msgstr` approximatif est comparé comme une traduction relue et peut apparaître dans un résultat. Relis ou nettoie les entrées fuzzy avant de te fier au rapport.
- Les clés de style pluriel dans un format sans notion de pluriel comptent comme des clés ordinaires. Les adaptateurs `yaml`, `ngx-translate-json`, `xliff`, `properties`, `ini` et `resx` ne connaissent pas les formes plurielles, donc `item_one` et `item_other` sont pour eux deux clés sans lien. Quand deux de ces formes d'une même clé partagent le texte source mais sont traduites différemment, elles apparaissent comme un résultat. Si ton format le permet, donne-leur une description ou un sens différents pour qu'elles ne soient jamais regroupées ; sinon, ignore le résultat.
- Les formats `custom:` n'ont pas de résolveur de formes plurielles. verbatra sait seulement comment ses adaptateurs intégrés nomment les formes plurielles, donc quand un adaptateur tiers range chaque forme sous sa propre clé, les formes d'une même clé sont comparées comme un seul groupe et peuvent apparaître comme un résultat si elles partagent le texte source. Donne aux formes une description ou un sens différents, ou ignore le résultat.

## Exemples

```bash
# fail the build when translations drifted
verbatra check

# machine-readable status for two locales
verbatra check --locales de,fr --json

# also list source strings translated more than one way
verbatra check --consistency
```

## Codes de sortie

| Code | Signification |
| --- | --- |
| `0` | synchronisé : aucune locale n'a de clé manquante ou obsolète |
| `1` | désynchronisé : au moins une locale a une clé manquante ou obsolète (le rapport est quand même affiché) |
| `2` | impossible de s'exécuter : une erreur globale, ou une erreur d'utilisation (y compris une valeur `--locales` vide ou inconnue) |

La sortie `1` signifie "désynchronisé", pas un échec : l'exécution elle-même a réussi, le résultat n'est juste pas tout vert.

Sur un projet dont le fichier de locale source n'est pas encore en place, `check` ne peut pas s'exécuter du tout : il échoue globalement avec `SOURCE_UNREADABLE` et sort avec `2`. [`verbatra doctor`](/docs/cli/doctor) valide la configuration du projet et rend quand même un rapport dans ce cas.

## Voir aussi

- [`verbatra diff`](/docs/cli/diff) liste les clés exactes derrière ces comptes.
- [CI et codes de sortie](/docs/ci-and-exit-codes) montre check comme contrôle de dérive.
- [Le fichier de verrouillage](/docs/the-lock-file) explique comment l'obsolescence est détectée.
